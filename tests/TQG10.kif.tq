(note TQG10)

(=>
  (instance ?VERT Vertebrate)
  (exists (?SPINE)
    (and
      (component ?SPINE ?VERT)
      (instance ?SPINE SpinalColumn))))
                        
(not
  (exists (?SPINE)
    (and
      (instance ?SPINE SpinalColumn)
      (part ?SPINE BananaSlug1))))

(partition Animal Vertebrate Invertebrate) ; this is in SUMO v1.55

(=>
    (partition ?SUPER ?SUB1 ?SUB2)
    (partition ?SUPER ?SUB2 ?SUB1))
    
(=>
  (and
    (partition ?SUPER ?SUB1 ?SUB2)
    (instance ?INST ?SUPER)
    (not
      (instance ?INST ?SUB1)))
  (instance ?INST ?SUB2))
  
(instance BananaSlug1 Animal)

(query (instance BananaSlug1 Invertebrate))

(answer Yes)
